# Заметки проекта «Кубок Авесто-2026»

Файл заметок по решениям и договорённостям проекта. Обновлять при значимых
изменениях.

## ⚠️ Единственный источник правды по командам

**Проблема (2026-07-16):** пользователь переименовал все 18 команд на
кириллицу и загрузил логотипы через админку в своём обычном браузере. Эти
правки живут **только в localStorage** этой конкретной сессии браузера.
Открыв сайт в режиме инкогнито или на другом устройстве, он увидел старые
английские названия (Zet-Mobile, Akia Avesto, Arter Avesto...) из
`src/data/seed.ts` — потому что чистый localStorage не содержит его правок,
только стартовый seed.

**Правило:** `src/data/seed.ts` — единственный источник правды на старте.
Всё, что вводится через UI админки (название, компания, лого, состав,
матчи), — временное состояние конкретного браузера, пока явно не перенесено
в код. Если пользователь хочет, чтобы правки команд «сохранились навсегда»
(видны всем, переживают инкогнито/деплой/сброс) — их нужно перенести в
`seed.ts` вручную.

**Как получить точный список команд из браузера пользователя** (у ассистента
нет доступа к его localStorage напрямую — только к своей собственной
browser-tooling сессии, которая НЕ то же самое, что браузер пользователя):

Попросить пользователя открыть свою рабочую вкладку (не инкогнито),
Cmd+Option+J (Mac Chrome) → напечатать `allow pasting` → Enter → вставить:

```js
copy(JSON.stringify(JSON.parse(localStorage.getItem('avesto-2026-tournament-data')).teams.map(t => ({id: t.id, name: t.name, company: t.company, group: t.group, hasLogo: !!t.logo})), null, 2))
```

Результат копируется в буфер — пользователь вставляет его в чат. Логотипы
(base64) через этот метод не получить — если логотип реально сменился (не
тот, что уже в `src/assets/logos/`), нужен отдельный файл от пользователя.

**Не полагаться на скриншоты/память из истории разговора** для восстановления
точных названий — риск ошибиться в написании (например «Артел Авесто» vs
«Артель Авесто»). Всегда просить свежий экспорт, если нужна 100% точность.

## Инструменты

- **2026-07-16**: попытка получить экспорт localStorage через
  `mcp__claude-in-chrome__javascript_tool` провалилась — эти browser-tools
  работают в собственной изолированной вкладке ассистента, не имеют доступа
  к localStorage реального браузера/профиля пользователя. Единственный
  рабочий способ — попросить пользователя вручную выполнить JS в консоли
  своего браузера и прислать результат текстом.

## Функциональность

- **2026-07-16**: создан пакет `.md`-файлов проекта (`ЗАПУСК.md`,
  `CLAUDE.md`, `DEPLOY.md`, `PRODUCT.md`, `DESIGN.md`, `MEMORY.md` — этот
  файл) по образцу другого проекта пользователя (`teamtask`), адаптирован
  под реалии этого сайта: без бэкенда, localStorage-only персистентность,
  единственный источник правды — `seed.ts`. Не копия чужого контента —
  структура позаимствована, содержание с нуля под этот проект.
- **2026-07-16**: убрано поле `jerseyColor` (цвет формы) из модели `Team`
  полностью — из формы создания/редактирования команды в админке, из
  `TeamPage` (фоновый оттенок хедера + индикатор «Форма»), из `TeamLogo`
  fallback (jersey-цветной бейдж заменён на нейтральные инициалы). Зона
  загрузки логотипа в админке переделана: dashed drop-зона с иконкой при
  пустом состоянии, превью + кнопки «Заменить»/«Убрать» при загруженном.
- **2026-07-16**: добавлена карточка «Чемпион турнира» в конец сетки
  плей-офф (`BracketPage.tsx`) — резолвится из победителя финального матча,
  показывает «TBD» пока финал не сыгран.
- **2026-07-16**: страница матчей (`MatchesPage.tsx`) переработана —
  группировка по дням + табы-фильтры «Предстоящие»/«Завершённые»/«Все»,
  дни чередуют фон (`bg-slate-800/50`/прозрачный) на всю ширину экрана, как
  секции лендинга — визуально легко отличить один день от другого.
- **2026-07-16**: расписание группового этапа генерируется алгоритмически
  (`buildGroupMatches()` в `seed.ts`) методом circle (стандартный
  round-robin scheduling) — гарантирует, что ни одна команда не играет
  дважды в один день. Дни чередуют пары групп A+B / C+D, по 2 матча на
  группу в день (4 матча/день). Есть кнопка «Пересобрать расписание» в
  `/admin/settings` — пересчитывает только матчи, не трогая
  команды/логотипы/составы (`rebuildGroupSchedule()` в `data/store.ts`).
- **2026-07-16**: карусель команд на главной (`TeamCarousel.tsx`) — багфикс
  «сходит с ума после рефреша»: `segmentWidthRef` кэшировался один раз при
  монтировании, до того как логотипы (async `<img>`) догружались и
  `scrollWidth` трека достигал финального значения — рассинхрон приводил к
  резким скачкам. Убран кэш, `segmentWidth` теперь пересчитывается заново
  на каждом scroll-событии. Отдельно: `scroll-smooth` класс на треке
  анимировал даже программные init/wrap-прыжки `scrollLeft` — убран из
  класса, `behavior: 'smooth'` передаётся явно только там, где нужна
  видимая анимация (кнопки, автопрокрутка).
- **2026-07-16**: `ScrollToTop` компонент (`src/components/ScrollToTop.tsx`)
  — react-router не скроллит наверх сам при смене маршрута, добавлен явный
  `window.scrollTo(0, 0)` на смену `pathname`, подключён в `App.tsx` внутри
  `BrowserRouter`.
- **2026-07-16**: сетка плей-офф (`BracketPage.tsx`) — соединительные линии
  между раундами переделаны на классический bracket-connector паттерн
  (угловые CSS-скобки через `border-r`/`border-b`/`border-t`), вместо
  JS-измерений позиций. Финал вертикально центрирован относительно ВСЕЙ
  сетки (обе пары четвертьфиналов), не только полуфиналов — коннектор
  SF→Финал растянут на полную высоту через убранный `py-10`/фиксированную
  высоту. «Матч за 3-е место» — отдельно позиционирован снизу через
  `absolute` внутри `relative` враппера, контейнер имеет
  `overflow-y-visible` (только `overflow-x-auto`), иначе абсолютный элемент
  обрезался.

## UI-договорённости

- Диалоги подтверждения — только кастомные `useConfirm()`/`useToast()`
  (`src/hooks/useConfirm.tsx`, `src/hooks/useToast.tsx`), никогда нативный
  `confirm()`/`alert()`. Применено везде в админке (2026-07-16).
- Формат дат везде на сайте — ДД.ММ.ГГГГ (`src/lib/formatDate.ts`), единый
  хелпер вместо разрозненных мест с сырым ISO `YYYY-MM-DD`.
- Логотип команды — только через `<TeamLogo>` компонент, никогда не
  инлайновая `<img>`-разметка в отдельных страницах (иначе визуальная
  рассинхронизация padding между компонентами, была реальная жалоба
  пользователя на «логотипы смещены»).
- Метка «Победа» в `MatchRow` — рендерится всегда (через `invisible` класс
  у непобедителя), не условно — иначе высота карточек в списке матчей
  скачет между строками с победителем и без.

## Мобильная адаптация

- Брейкпоинт desktop-навигации в шапке — `xl:` (1280px), не `md:`/`lg:` —
  на разрешениях 1024–1279px полная навигация (6 пунктов) физически не
  помещается, обрезает кнопку «Админ».
- Sidebar-формы в админке (список + форма, `grid-cols-[1fr_320px]`) —
  переключение на `xl:`, та же причина (тесно на 1024–1279px).
- Все поля форм — минимум 16px шрифта (`input, select, textarea` в
  `index.css`) — иначе iOS Safari зумит страницу при фокусе на поле,
  независимо от `text-sm`/`text-xs` в разметке.
- Touch-таргеты кнопок-иконок в админке — минимум 44×44px
  (`min-h-11 min-w-11`), стандарт WCAG/Apple HIG.
- `viewport-fit=cover` в `index.html` + `env(safe-area-inset-*)` в
  `index.css` (`.site-header`, `.safe-bottom`, `body` left/right) — под
  «остров»/notch iPhone и системные жесты Android.
- Полный мобильный аудит проведён 2026-07-16 (делегирован фоновому агенту) —
  нашёл и починил: горизонтальный overflow от countdown-таймера на узких
  экранах, таблицы без scroll-контейнера (`GroupTable`, рейтинг
  бомбардиров), toast без учёта safe-area снизу.

## Данные и персистентность

- **Нет бэкенда.** Все данные — `localStorage`, ключ
  `avesto-2026-tournament-data` (см. `src/data/store.ts`). Первый заход
  сидит из `src/data/seed.ts`, дальше читает/пишет напрямую localStorage.
- **Производные данные не хранятся.** Турнирная таблица (`standings.ts`),
  сетка плей-офф (`playoff.ts`), рейтинг бомбардиров (`scorers.ts`) —
  всегда пересчитываются через `useMemo` из текущих matches/teams/players
  при каждом рендере. Никогда не кэшируются как отдельное поле в данных —
  так исключается рассинхронизация.
- **Изображения (логотипы/фото)** — base64, сжимаются на клиенте через
  canvas resize + WebP re-encode перед сохранением (`fileToBase64` в
  `store.ts`, `MAX_IMAGE_DIMENSION=400`, `IMAGE_QUALITY=0.85`) — квота
  localStorage общая на весь турнир сразу, без сжатия быстро упирается в
  лимит браузера.
- **Ошибка переполнения localStorage** обрабатывается явно —
  `saveData()` бросает описательную ошибку, `useTournamentData` откатывает
  состояние и показывает alert, вместо падения всего приложения в
  необработанном useEffect.

## Лог изменений

(новые записи сверху)

- 2026-07-16 — кириллические названия команд перенесены в `seed.ts` из точного JS-экспорта localStorage пользователя (не по скриншотам). Изменились: Zet-Mobile→Зет-Мобайл, Siyoma→Сиёма, Cityline→Сити Лайн, Neru→Неру, Arvis→Арвис, Akia Avesto→Акиа Авесто, City Service→Сити Сервис, **KOD→Сафо** (company осталась «ООО «КОД»» — рассинхрон в исходных данных пользователя, не трогали), Olu4a Taxi→Олуча Такси, Avesto→Авесто Групп, **DC→Душанбе Сити Банк**, Azot→Азот, NETS→НЕТС, Siyoma Mall→Сиёма Молл, Muosir→Муосир, Arter Avesto→Артел Авесто (без мягкого знака — так в экспорте пользователя), Marmari→Мармари.
- 2026-07-16 — **логотипы всех 16 команд заменены на новые** (пользователь их менял через админку, старые в `logos.ts` не совпадали — экспорт давал только `hasLogo:true/false`, не сам файл, флаг не различает старый/новый лого). Пользователь прислал 17 оригинальных PNG-файлов (один дубль — DC Маркет и Душанбе Сити Банк используют один и тот же логотип, подтверждено). Файлы лежали на Desktop в `logocomp/`, размеры до 15348×3692px/4.3MB — ужаты через `sips -Z 400` (макс. сторона 400px, та же величина что `MAX_IMAGE_DIMENSION` в `store.ts`) и перекодированы в base64 PNG (не WebP — на машине не было `cwebp`/`sharp`, WebP-квант из `fileToBase64` не воспроизведён этим путём, при следующей загрузке лого через админку сожмётся в WebP как обычно). Заменены константы `LOGO_*` в `src/data/logos.ts` напрямую по base64. Bundle вырос 746KB→1.16MB (новые лого крупнее старых). Визуально сверено на dev-сервере (localStorage очищен в отдельном браузерном профиле → пересидился из обновлённого `seed.ts`) — все 18 карточек на `/teams` совпадают с присланными файлами. tsc/vitest/build — зелёные.
- 2026-07-16 — 5 правок: сортировка матчей по дате+времени (админка, /matches, "Ближайшие матчи" на главной), призовой фонд 30000→60000 сомони, поле "Дата рождения"→"№ приказа" в форме игрока (date-picker→text), новый логотип Avesto (вертикальный формат, `src/assets/brand/avesto-group-logo.png` заменён, позиция/размер в hero подогнаны).
- 2026-07-16 — созданы ЗАПУСК.md/CLAUDE.md/DEPLOY.md/PRODUCT.md/DESIGN.md/MEMORY.md.
- 2026-07-16 — попытка перенести кириллические названия команд пользователя в `seed.ts` — в процессе, ждём точный экспорт localStorage от пользователя (см. раздел «Единственный источник правды» выше).
- 2026-07-16 — мобильный адаптив: брейкпоинт nav/sidebar-форм поднят с md/lg на xl (не помещались на 1024–1279px), фикс горизонтального overflow от countdown-таймера, таблицы обёрнуты в overflow-x-auto, safe-area на toast.
- 2026-07-16 — сетка плей-офф: bracket-connector линии, финал центрирован по всей сетке (не только полуфиналам), карточка «Чемпион турнира» добавлена.
- 2026-07-16 — убрано поле jerseyColor из модели Team полностью, редизайн зоны загрузки логотипа в админке.
- 2026-07-16 — расписание группового этапа: алгоритмическая генерация без коллизий команд в один день, кнопка «Пересобрать расписание».
- 2026-07-16 — багфиксы: карусель команд после рефреша, ScrollToTop на смену маршрута, метка «Победа» без сдвига layout.

---
Последнее обновление: 2026-07-16
